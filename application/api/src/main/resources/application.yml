spring:
  application:
    name: api
  profiles:
    active: local

  config:
    import:
      - logging.yml

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: staging

---
spring:
  config:
    activate:
      on-profile: prod
